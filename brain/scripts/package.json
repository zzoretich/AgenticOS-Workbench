{
  "name": "agenticos-brain",
  "private": true,
  "version": "0.19.2",
  "type": "commonjs",
  "description": "AgenticOS brain runtime: Claude Code hooks, vault scanner, BRAIN.md compiler, recall, and the agenticos MCP server.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "NODE_OPTIONS=--require=./test/setup.js node --test test/*.test.js",
    "test:live": "for f in test/live/test-*.js; do node \"$f\" || exit 1; done"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  }
}
