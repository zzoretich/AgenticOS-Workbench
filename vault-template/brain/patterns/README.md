# Patterns

Decision heuristics, one file per area (`debugging.md`, `architecture.md`, …), each with `### <pattern title>` subsections. Written by `/pattern`; the H1 of every file is listed in `brain/_index/MOC-patterns.md` on every scan.
