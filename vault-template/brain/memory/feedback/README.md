# Feedback rules

One file per rule about how to work with you: `## Rule`, `## Why`, `## How to apply`. Written by `/feedback` and by approving drafts (the `feedback-review` skill). Auto-drafted rules wait in `_drafts/` and are not indexed until approved. Indexed under `## Feedback (how to work)` in `MEMORY.md`.
