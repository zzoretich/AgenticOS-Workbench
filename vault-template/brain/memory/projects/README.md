# Project memories

One file per project: `## Summary`, `## Context`, `## Updates`. Tag `status/active` in the frontmatter to have it compiled into `BRAIN.md` `## Active context`; switch to `status/complete` to retire it. Indexed under `## Project` in `MEMORY.md`.
