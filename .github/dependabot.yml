# Keeps the commit-pinned GitHub Actions current (spec 2026-09-23-ci-safety-net-design D9): each update is a pull
# request that runs the full CI before anyone merges it.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
